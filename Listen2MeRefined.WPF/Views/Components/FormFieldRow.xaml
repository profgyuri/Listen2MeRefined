<UserControl x:Class="Listen2MeRefined.WPF.Views.Components.FormFieldRow"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Style="{StaticResource SettingsFieldLabelText}"
                   Text="{Binding Label, ElementName=Root}"
                   Width="{Binding LabelWidth, ElementName=Root}"
                   VerticalAlignment="Center" />

        <ContentPresenter Grid.Column="1"
                          VerticalAlignment="Center"
                          Content="{Binding FieldContent, ElementName=Root}" />
    </Grid>
</UserControl>
