<UserControl x:Class="Listen2MeRefined.WPF.Views.MainWindow.SearchBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:utils="clr-namespace:Listen2MeRefined.WPF.Utils">
    <Grid utils:ViewModelInitialization.AutoInitialize="True" Margin="0 10 0 0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBox Text="{Binding SearchTerm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" materialDesign:HintAssist.Hint="Quick Search" BorderBrush="{StaticResource TertiaryBrush}" materialDesign:TextFieldAssist.UnderlineBrush="{StaticResource TertiaryBrush}">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding QuickSearchCommand}" />
            </TextBox.InputBindings>
        </TextBox>
        <Button Grid.Column="1" Margin="20 0 0 0" Command="{Binding QuickSearchCommand}" Style="{StaticResource InteractionButton}" Content="{materialDesign:PackIcon Kind=Search,Size=20}" ToolTip="Quick Search" />
        <Button Grid.Column="2" Margin="20 0 0 0" Style="{StaticResource InteractionButton}" Content="{materialDesign:PackIcon Kind=BookOpenOutline}" ToolTip="Advanced Search" Command="{Binding DataContext.OpenAdvancedSearchWindowCommand, RelativeSource={RelativeSource AncestorType=Window}}" />

    </Grid>
</UserControl>
