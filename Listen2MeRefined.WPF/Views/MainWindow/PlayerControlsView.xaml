<UserControl x:Class="Listen2MeRefined.WPF.Views.MainWindow.PlayerControlsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:wpf="clr-namespace:Listen2MeRefined.WPF"
             xmlns:utils="clr-namespace:Listen2MeRefined.WPF.Utils">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Padding="10" Margin="0 0 10 0" Style="{StaticResource ContainerBorder}">
            <Grid VerticalAlignment="Center" Margin="5 0 10 5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" TextTrimming="CharacterEllipsis" FontSize="26" Text="{Binding Song.Title, FallbackValue=Title}" />
                <TextBlock Grid.Row="1" Margin="0 5" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold" FontStyle="Italic" TextTrimming="CharacterEllipsis" FontSize="20" Text="{Binding Song.Artist, FallbackValue=Artist}" />

                <UniformGrid Grid.Row="2" Columns="3" Margin="0 0 0 5">
                    <TextBlock FontSize="14" Margin="0 0 0 -10" Text="{Binding Song.BPM, StringFormat={}{0} BPM, FallbackValue=0 BPM}" />
                    <TextBlock FontSize="14" Margin="0 0 0 -10" HorizontalAlignment="Center" Text="{Binding Song.Genre, FallbackValue=Genre}" />
                    <TextBlock Margin="0 0 0 -10" HorizontalAlignment="Right" FontSize="14" Text="{Binding Song.Bitrate, StringFormat={}{0} Kbps, FallbackValue=1411 kbps}" />
                </UniformGrid>
            </Grid>
        </Border>

        <Border DataContext="{Binding PlayerControlsViewModel}" utils:ViewModelInitialization.AutoInitialize="True" Grid.Column="1" Style="{StaticResource ContainerBorder}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Command="{Binding PreviousCommand}" Style="{StaticResource PlayerButton}" Content="{materialDesign:PackIcon Kind=SkipPrevious,Size=20}"/>
                <Button Grid.Column="1" Command="{Binding StopCommand}" Style="{StaticResource PlayerButton}" Content="{materialDesign:PackIcon Kind=Stop,Size=20}" />
                <Button Grid.Column="2" Command="{Binding PlayPauseCommand}" Style="{StaticResource PlayerButton}" Content="{materialDesign:PackIcon Kind=PlayPause,Size=20}" />
                <Button Grid.Column="3" Command="{Binding NextCommand}" Style="{StaticResource PlayerButton}" Content="{materialDesign:PackIcon Kind=SkipNext,Size=20}" />
                <Button Grid.Column="4" Command="{Binding ShuffleCommand}" Style="{StaticResource PlayerButton}" Content="{materialDesign:PackIcon Kind=Shuffle,Size=20}" />
                <wpf:WaveSlider Style="{StaticResource NoHaloSlider}" Foreground="{StaticResource TertiaryBrush}" WaveBitmap="{Binding WaveForm}" Grid.Row="1" Minimum="0" Maximum="{Binding TotalTime}" Value="{Binding CurrentTime}" Grid.ColumnSpan="5" Width="{Binding WaveFormWidth}" Height="{Binding WaveFormHeight}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
