<UserControl x:Class="Listen2MeRefined.WPF.Views.MainWindow.TitleBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d">
    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="35" />
            <ColumnDefinition Width="35" />
            <ColumnDefinition Width="35" />
            <ColumnDefinition Width="35" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Listen2Me" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.ColumnSpan="4" FontWeight="Bold" />

        <Button Grid.Column="1" Command="{Binding OpenSettingsWindowCommand}" Style="{StaticResource TitleBarButton}" Content="{materialDesign:PackIcon Kind=Gear,Size=15}" />
        <TextBlock IsHitTestVisible="False" Grid.Column="1" Foreground="Red" Visibility="{Binding IsUpdateAvailable, Converter={StaticResource BoolToVisibilityConverter}}" HorizontalAlignment="Right" VerticalAlignment="Top" FontWeight="Black" Padding="0 1 2 0" Text="!"/>
        <Button Grid.Column="2" Padding="0 0 0 5" Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=MinimizeWindowCommand}" Style="{StaticResource TitleBarButton}" Content="{materialDesign:PackIcon Kind=WindowMinimize,Size=15}" VerticalContentAlignment="Bottom" />
        <Button Grid.Column="3" Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=ToggleWindowStateCommand}" Style="{StaticResource TitleBarButton}" Content="{materialDesign:PackIcon Kind=WindowMaximize,Size=15}" />
        <Button Grid.Column="4" Command="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=CloseWindowCommand}" Content="{materialDesign:PackIcon Kind=Close,Size=15}" Style="{StaticResource WindowCloseButton}" materialDesign:ButtonAssist.CornerRadius="0 8 0 0" />
    </Grid>
</UserControl>
