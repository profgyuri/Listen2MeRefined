<UserControl x:Class="Listen2MeRefined.WPF.Views.MainWindow.MainTabsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:views="clr-namespace:Listen2MeRefined.WPF.Views.MainWindow">
    <TabControl Margin="0,20,10,14"
                Background="Transparent"
                BorderThickness="0"
                materialDesign:ColorZoneAssist.Mode="Custom"
                ItemContainerStyle="{StaticResource MainTabsSegmentTabItemStyle}">

        <TabItem Header="Search Results" IsSelected="{Binding IsSearchResultsTabVisible}" Cursor="Hand">
            <views:SearchResultsPaneView DataContext="{Binding DataContext.SearchResultsPaneViewModel, RelativeSource={RelativeSource AncestorType=Window}}" />
        </TabItem>

        <TabItem Header="Song Menu" IsSelected="{Binding IsSongMenuTabVisible}" Cursor="Hand">
            <Border Background="#282828"
                    BorderBrush="#3A3A3A"
                    BorderThickness="1"
                    CornerRadius="12"
                    Margin="0,8,0,0"
                    Padding="14">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Background="#232323" BorderBrush="#383838" BorderThickness="1" CornerRadius="10" Padding="12">
                            <StackPanel>
                                <TextBlock Style="{StaticResource MainTabsCardTitleText}" Text="Playback Actions" />
                                <Button Style="{StaticResource MainTabsActionButton}" Command="{Binding JumpToSelectedSongCommand}" Content="Jump To This Song" />
                                <Button Style="{StaticResource MainTabsActionButton}" Command="{Binding SetSelectedSongAsNextCommand}" Content="Set As Next Song" Margin="0" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="2" Background="#232323" BorderBrush="#383838" BorderThickness="1" CornerRadius="10" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="2*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Style="{StaticResource MainTabsCardTitleText}" Text="Song Details" />
                                    <TextBox Text="{Binding SelectedSong.Artist}" Margin="0,0,0,10" materialDesign:HintAssist.Hint="Artist" />
                                    <TextBox Text="{Binding SelectedSong.Title}" Margin="0,0,0,10" materialDesign:HintAssist.Hint="Title" />
                                    <TextBox Text="{Binding SelectedSong.Genre}" materialDesign:HintAssist.Hint="Genre" />
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Style="{StaticResource MainTabsCardTitleText}" Text="Audio Meta" />
                                    <Border Background="#2C2C2C" BorderBrush="#404040" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Style="{StaticResource MainTabsMetaLabelText}" Text="BITRATE" />
                                            <TextBlock Style="{StaticResource MainTabsMetaValueText}" Text="{Binding SelectedSong.Bitrate}" />
                                        </StackPanel>
                                    </Border>
                                    <Border Background="#2C2C2C" BorderBrush="#404040" BorderThickness="1" CornerRadius="8" Padding="10">
                                        <StackPanel>
                                            <TextBlock Style="{StaticResource MainTabsMetaLabelText}" Text="BPM" />
                                            <TextBlock Style="{StaticResource MainTabsMetaValueText}" Text="{Binding SelectedSong.BPM}" />
                                        </StackPanel>
                                    </Border>
                                    <Button Style="{StaticResource MainTabsActionButton}" Command="{Binding ScanSelectedSongCommand}" Content="Scan This Song" Margin="0,10,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Row="4" Background="#232323" BorderBrush="#383838" BorderThickness="1" CornerRadius="10" Padding="12">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="DeleteOutline" Width="18" Height="18" Foreground="{StaticResource TertiaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <Button Style="{StaticResource MainTabsActionButton}" Command="{Binding RemoveSelectedFromPlaylistCommand}" Content="Remove From Playlist" Margin="0" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </Border>
        </TabItem>
    </TabControl>
</UserControl>

