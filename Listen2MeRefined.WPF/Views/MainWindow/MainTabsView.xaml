<UserControl x:Class="Listen2MeRefined.WPF.Views.MainWindow.MainTabsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:mainWindow="clr-namespace:Listen2MeRefined.Infrastructure.ViewModels.MainWindow;assembly=Listen2MeRefined.Infrastructure"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance mainWindow:ListsViewModel}"
             xmlns:views="clr-namespace:Listen2MeRefined.WPF.Views.MainWindow">
    <TabControl Margin="0,20,10,14"
                Background="Transparent"
                BorderThickness="0"
                materialDesign:ColorZoneAssist.Mode="Custom"
                ItemContainerStyle="{DynamicResource MainTabsSegmentTabItemStyle}">

        <TabItem Header="Search Results" IsSelected="{Binding IsSearchResultsTabVisible}" Cursor="Hand">
            <views:SearchResultsPaneView DataContext="{Binding DataContext.SearchResultsPaneViewModel, RelativeSource={RelativeSource AncestorType=Window}}" />
        </TabItem>

        <TabItem Header="Song Menu" IsSelected="{Binding IsSongMenuTabVisible}" Cursor="Hand">
            <Border Style="{DynamicResource BorderContainer}"
                    Margin="0,8,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="12" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Style="{DynamicResource DarkBorderContainer}">
                            <StackPanel>
                                <TextBlock Style="{DynamicResource MainTabsCardTitleText}" Text="Playback Actions" />
                                <Button Style="{DynamicResource MainTabsActionButton}" Command="{Binding JumpToSelectedSongCommand}" Content="Jump To This Song" />
                                <Button Style="{DynamicResource MainTabsActionButton}" Command="{Binding SetSelectedSongAsNextCommand}" Content="Set As Next Song" Margin="0" />
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="2" Style="{DynamicResource DarkBorderContainer}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="2*" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                    <TextBlock Style="{DynamicResource MainTabsCardTitleText}" Text="Song Details" />
                                    <TextBox Text="{Binding SelectedSong.Artist}" Margin="0,0,0,10" materialDesign:HintAssist.Hint="Artist" />
                                    <TextBox Text="{Binding SelectedSong.Title}" Margin="0,0,0,10" materialDesign:HintAssist.Hint="Title" />
                                    <TextBox Text="{Binding SelectedSong.Genre}" materialDesign:HintAssist.Hint="Genre" />
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Style="{DynamicResource MainTabsCardTitleText}" Text="Audio Meta" />
                                    <Border Style="{DynamicResource BorderContainer}" Margin="0,0,0,8">
                                        <StackPanel>
                                            <TextBlock Style="{DynamicResource MainTabsMetaLabelText}" Text="BITRATE" />
                                            <TextBlock Style="{DynamicResource MainTabsMetaValueText}" Text="{Binding SelectedSong.Bitrate}" />
                                        </StackPanel>
                                    </Border>
                                    <Border Style="{DynamicResource BorderContainer}">
                                        <StackPanel>
                                            <TextBlock Style="{DynamicResource MainTabsMetaLabelText}" Text="BPM" />
                                            <TextBlock Style="{DynamicResource MainTabsMetaValueText}" Text="{Binding SelectedSong.BPM}" />
                                        </StackPanel>
                                    </Border>
                                    <Button Style="{DynamicResource MainTabsActionButton}" Command="{Binding ScanSelectedSongCommand}" Content="Scan This Song" Margin="0,10,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Row="4" Style="{DynamicResource DarkBorderContainer}">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="DeleteOutline" Width="18" Height="18" Foreground="{DynamicResource TertiaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <Button Style="{DynamicResource MainTabsActionButton}" Command="{Binding RemoveSelectedFromPlaylistCommand}" Content="Remove From Playlist" Margin="0" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </ScrollViewer>
            </Border>
        </TabItem>
    </TabControl>
</UserControl>

