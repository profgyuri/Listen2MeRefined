<Window x:Class="Listen2MeRefined.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Listen2MeRefined.Infrastructure.Mvvm;assembly=Listen2MeRefined.Infrastructure"
        xmlns:views="clr-namespace:Listen2MeRefined.WPF.Views.MainWindow"
        xmlns:mainWindow="clr-namespace:Listen2MeRefined.Infrastructure.Mvvm.MainWindow;assembly=Listen2MeRefined.Infrastructure"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance mainWindow:MainWindowViewModel}"
        Style="{StaticResource BasicWindow}"
        WindowStartupLocation="Manual"
        Title="{Binding Song.Display}"
        MinHeight="502" MinWidth="1030"
        Height="702" Width="1030">

    <WindowChrome.WindowChrome>
        <WindowChrome x:Name="WindowChrome" CaptionHeight="32" />
    </WindowChrome.WindowChrome>

    <Border BorderThickness="2" BorderBrush="{StaticResource TertiaryBrush}" Background="{StaticResource PrimaryBrush}" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="{Binding ElementName=WindowChrome, Path=CaptionHeight, FallbackValue=32}" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <views:TitleBarView />

            <Grid Grid.Row="1" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" MinHeight="150" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.ColumnSpan="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <views:TrackInfoView />
                    <views:PlaybackControlsView Grid.Column="1" />
                </Grid>

                <views:MainTabsView DataContext="{Binding ListsViewModel}" Grid.Row="1" />

                <views:PlaylistPaneView DataContext="{Binding PlaylistPaneViewModel}" Grid.Row="1" Grid.Column="1" />
                <views:SearchBarView DataContext="{Binding SearchbarViewModel}" Grid.Row="2" Grid.ColumnSpan="2" />
            </Grid>
        </Grid>
    </Border>
</Window>
