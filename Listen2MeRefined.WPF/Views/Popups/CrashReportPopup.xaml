<UserControl x:Class="Listen2MeRefined.WPF.Views.Popups.CrashReportPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="360"
             d:DesignWidth="620">
    <Grid Margin="14,12,14,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource SecondaryBrush}"
                   Text="Listen2Me encountered an unexpected error." />

        <TextBlock Grid.Row="1"
                   Margin="0,10,0,0"
                   Foreground="{DynamicResource SecondaryDarkBrush}"
                   TextWrapping="Wrap"
                   Text="{Binding SummaryText}" />

        <Border Grid.Row="2"
                Margin="0,14,0,0"
                Padding="10"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SecondaryBrush}"
                           Text="Source:" />
                <TextBlock Grid.Row="0"
                           Grid.Column="1"
                           FontFamily="Consolas"
                           Foreground="{DynamicResource SecondaryDarkBrush}"
                           Text="{Binding SourceText}" />

                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           Margin="0,6,0,0"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SecondaryBrush}"
                           Text="Occurred (UTC):" />
                <TextBlock Grid.Row="1"
                           Grid.Column="1"
                           Margin="0,6,0,0"
                           FontFamily="Consolas"
                           Foreground="{DynamicResource SecondaryDarkBrush}"
                           Text="{Binding OccurredAtText}" />

                <TextBlock Grid.Row="2"
                           Grid.Column="0"
                           Margin="0,6,0,0"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource SecondaryBrush}"
                           Text="Log folder:" />
                <TextBlock Grid.Row="2"
                           Grid.Column="1"
                           Margin="0,6,0,0"
                           FontFamily="Consolas"
                           Foreground="{DynamicResource SecondaryDarkBrush}"
                           TextWrapping="Wrap"
                           Text="{Binding LogDirectoryPath}" />
            </Grid>
        </Border>

        <TextBlock Grid.Row="3"
                   Margin="0,14,0,6"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource SecondaryBrush}"
                   Text="Exception" />

        <TextBox Grid.Row="4"
                 Background="{DynamicResource SurfaceBrush}"
                 Foreground="{DynamicResource SecondaryDarkBrush}"
                 BorderThickness="0"
                 Padding="10"
                 FontFamily="Consolas"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 Text="{Binding ExceptionDetails}" />
    </Grid>
</UserControl>
